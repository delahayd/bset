#equal_image_26.zf

include "bset.zf".

goal forall (a b : type) (S : set a) (T :set b) (R : set (tup a b)).
  mem (set (tup a b)) R (relation a b S T) => image b a (inverse a b R) (ran a b R) = dom a b R.