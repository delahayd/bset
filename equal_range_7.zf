# equal_range_7.zf

include "bset.zf".

goal forall (a b : type) (S : set a) (T V : set b) (R : set (tup a b)).
  mem (set (tup a b)) R (relation a b S T) && subset b V T => ran a b (range_restriction a b R V) = inter b (ran a b R) V.