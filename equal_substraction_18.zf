# equal_substraction_18.zf

include "bset.zf".

goal forall (a b : type) (S U : set a) (R : set (tup a b)).
  subset a U S && subset a (dom a b R) U => (domain_substraction a b U R = empty (tup a b)).
