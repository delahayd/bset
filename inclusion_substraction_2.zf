# inclusion_substraction_2.zf

include "bset.zf".

goal forall (a b : type) (S : set a) (T V: set b) (R : set (tup a b)).
 mem (set (tup a b)) R (relation a b S T) && subset b T V  => subset (tup a b) (range_substraction a b R V) R.