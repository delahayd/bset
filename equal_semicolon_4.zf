# equal_semicolon_4.zf

include "bset.zf".

goal forall (a b : type) (S : set a) (T V : set b) (R : set (tup a b)).
  mem (set (tup a b)) R (relation a b S T) && subset b V T => semicolon a b b R (id b V) = range_restriction a b R V.