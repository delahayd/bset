#equal_image_17.zf

include "bset.zf".

goal forall (a b : type) (S U : set a) (T :set b) (R : set (tup a b)).
  mem (set (tup a b)) R (relation a b S T) && inter a (dom a b R) U = empty a => image a b R U = empty b.