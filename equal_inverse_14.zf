#equal_inverse_14.zf

include "bset.zf".

goal forall (a b : type) (S : set a) (T : set b) (R : set (tup a b)).
  mem (set (tup a b)) R (relation a b S T) && R = empty (tup a b) => inverse a b R = empty (tup b a).